/** Marker for a configured SecretRef whose concrete value is unavailable. */
export const AGENT_SECRET_REF_CONFIGURED_MARKER = "openclaw-secret-ref-configured";

/** Marker for a secret-ref-managed credential that is not stored as an env var. */
export const NON_ENV_SECRETREF_MARKER = "secretref-managed"; // pragma: allowlist secret
